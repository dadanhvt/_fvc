
(function() {
    'use strict';

    angular
        .module('phaojlar.layouts')
        .controller('DefaultMenuLayoutController', DefaultMenuLayoutController);

    /* @ngInject */
    function DefaultMenuLayoutController($rootScope) {
        var vm = this;
        init();
        function init(){
        }
    }
})();
